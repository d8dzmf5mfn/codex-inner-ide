# Codex Desktop 26.721.41059 compatibility

Status: **validated profile; Browser-first handoff passed**

Inspected on 2026-07-25 from the installed `/Applications/ChatGPT.app`:

- Codex Desktop: `26.721.41059` (bundle build `5848`)
- Bundled CLI: `codex-cli 0.146.0-alpha.3.1`
- App ASAR SHA-256: `da39a51b06fb4c728d418b8f0f05fc8fd8c6b1f74c4fb4d47c20c7914a798f45`
- The packaged ASAR retains `composer-surface-chrome`,
  `data-codex-composer-root`, `data-codex-composer`,
  `quick-chat-prewarm`, `selectedTextOverlay.quickChatPrompt`, and
  `avatar-overlay`.
- The bundled CLI retains the experimental `app-server` command and stdio
  transport used by Codex Inner Edit.
- The installed Codex bundle passes strict deep signature verification.

The profile reuses `desktop-v1`. Runtime checks still require a matching main
`app://` target, a visible Files row, and a dedicated ChatGPT Quick Chat
composer. A mismatch fails closed to the native IDE or clipboard handoff.

The Browser-first route was exercised against the installed Codex build on
2026-07-25. This Codex build replaced the requested debugging port with its own
random loopback port; the Companion recovered the actual port from the running
process, connected to the main `app://` renderer, completed the authenticated
Browser handshake, and opened `Codex Inner IDE` on a separate random loopback
port. The Browser open call now carries an explicit CDP user gesture and focuses
the created window so the updated popup behavior remains reliable. The live CDP
recovery and main-renderer tests passed.

Browser-first and Sidepanel behavior remain preview compatibility behavior.
Unknown versions and failed handshakes use the native IDE.

The controller does not modify the Codex app, ASAR, signature, or private IPC.

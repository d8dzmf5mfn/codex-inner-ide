# Codex Desktop 26.715.72028 compatibility

Status: **local package profile enabled; live CDP handoff verification pending**

Validated on 2026-07-22 from the installed `/Applications/ChatGPT.app`:

- Codex Desktop: `26.715.72028` (bundle build `5706`)
- Bundled CLI: `codex-cli 0.145.0-alpha.30`
- The packaged ASAR still contains the current composer markers, including
  `composer-surface-chrome` and `data-codex-composer-root`.
- The packaged ASAR still contains Quick Chat, selected-text overlay, and
  `selectedTextOverlay.quickChatPrompt` markers.
- Live CDP inspection confirms Quick Chat creates a short-lived prewarm target,
  while the actual composer is the focused `Message ChatGPT` ProseMirror in the
  main renderer. It is distinguished from the Codex composer by the absence of
  `data-codex-composer` and the presence of `form[data-thread-find-composer]`.
- The Python App Server protocol version is unchanged from the previous validated
  `0.145.0-alpha.30` profile.

The profile keeps the existing `desktop-v1` selectors. The controller still
fails closed if the main `app://` renderer, Files row, composer, or strict Quick
Chat marker checks do not match at runtime.

The controller does not modify the Codex app, ASAR, signature, or private IPC.

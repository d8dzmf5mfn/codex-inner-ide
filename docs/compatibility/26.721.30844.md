# Codex Desktop 26.721.30844 compatibility

Status: **candidate profile enabled; static compatibility audit passed**

Inspected on 2026-07-24 from the installed `/Applications/ChatGPT.app`:

- Codex Desktop: `26.721.30844` (bundle build `5813`)
- Bundled CLI: `codex-cli 0.146.0-alpha.3`
- App ASAR SHA-256: `29dac6cff7bc1aa39c64b4f12c0e47b07bccd0db11767ebc94c1196389b90619`
- The packaged ASAR retains `composer-surface-chrome`,
  `data-codex-composer-root`, `data-codex-composer`,
  `quick-chat-prewarm`, `selectedTextOverlay.quickChatPrompt`, and
  `avatar-overlay`.
- The bundled CLI retains the experimental `app-server` command and stdio
  transport used by Codex Inner Edit.

The profile reuses `desktop-v1`. Runtime checks still require a matching main
`app://` target, a visible Files row, and a dedicated ChatGPT Quick Chat composer.
A mismatch fails closed to the native IDE or clipboard handoff.

The Browser-first target and Sidepanel handshake remain preview compatibility
behavior. Unknown versions and failed handshakes use the native IDE.
An end-to-end click on the injected Sidepanel row was not performed because the
Mac was locked during release validation; the profile is enabled from the
packaged-marker audit, compatibility tests, and real `app-server` integration
test evidence.

The controller does not modify the Codex app, ASAR, signature, or private IPC.
